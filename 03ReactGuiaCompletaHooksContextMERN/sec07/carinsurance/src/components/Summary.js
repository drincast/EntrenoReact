import React, { Fragment } from 'react';

const Summary = ({ data }) => {
    const { brand, year, plan }

    return (
        <Fragment>
            <h1>resumen !!!!!</h1>
        </Fragment>
    );
}

export default Summary;